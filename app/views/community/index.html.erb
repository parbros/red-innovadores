<div class="row">
  <div class="span12">
    <p class='lead'>En esta sección, te invitamos a compartir tus experiencias e ideas innovadoras sobre temas sociales y políticos, utilizando videos, imagenes, PDFs, etc.</p>
  </div>
</div>
<div class="row">

  <div class="span6">
    <h1> Experiencias 
      <% if current_refinery_user %>
        <a href="/experiences/new" class="btn btn-warning pull-right" data-toggle="modal">Nueva Experiencia</a>
      <% else %>
        <a href="/refinery/users/login" class="btn btn-warning pull-right" data-toggle="modal">Nueva Experiencia</a>
      <% end %>
    </h1>
    
    <div id="experiences" class="row">
      <% @experiences.each do |experience| %>
      <div class="span6">
        <div class="experience">
          <div class="experience-container">
            <aside class='details'>
              <div class="details-container">
                <div class="date">
                  <%= l(experience.created_at.to_date, :format => :short)%> <span><%= experience.created_at.year%></span>
                </div>
                
                <div class="details-separator"></div>

                <div class="author">
                  <span><i class="icon-user"></i><%= "#{experience.user.username.humanize}" if experience.user.present? %></span>
                </div>
              </div>
            </aside>
            <section class="summary">
              <div class="summary-container">
                <header>
                  <h2><%= link_to experience.title, refinery.experiences_experience_path(experience) %> </h2>
                </header>

                <p><%= content_teaser(experience).html_safe %></p>
                <%= link_to refinery.experiences_experience_path(experience) do %>
                  Leer más <%= image_tag '/assets/read-more.png'%>
                <% end %>
              </div>
            </section>
          </div>
        </div>
      </div>
      <% end %>
    </div>
  </div>

  <div class="span6">
    <h1> Ideas 
      <% if current_refinery_user %>
        <a href="/ideas/new" class="btn btn-warning pull-right" data-toggle="modal">Nueva Idea</a>
      <% else %>
        <a href="/refinery/users/login" class="btn btn-warning pull-right" data-toggle="modal">Nueva Idea</a>
      <% end %>
    </h1>
    <div id="ideas">
      <% @ideas.each do |idea| %>
        <div class="span6">
          <div class="idea">
            <div class="idea-container">
              <aside class='details'>
                <div class="details-container">
                  <div class="date">
                    <%= l(idea.created_at.to_date, :format => :short)%> <span><%= idea.created_at.year%></span>
                  </div>
                  
                  <div class="details-separator"></div>
                  
                  <div class="author">
                    <span><i class="icon-user"></i><%= "#{idea.user.username.humanize}" if idea.user.present? %></span>
                  </div>
                  
                </div>
              </aside>
              <section class="summary">
                <div class="summary-container">
                  <header>
                    <h2><%= link_to idea.title, refinery.ideas_idea_path(idea) %></h2>
                  </header>

                  <p><%= content_teaser(idea).html_safe %></p>
                  <%= link_to refinery.ideas_idea_path(idea) do %>
                    Leer más <%= image_tag '/assets/read-more.png'%>
                  <% end %>
                </div>
              </section>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>